<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MSAMobApp.Views.NewStockItem">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
           
            <Entry x:Name="scanResultText" Placeholder="Scan BarCode" 
                   Text="{Binding BarCode, Mode=TwoWay}" />
            
            <Entry Placeholder="Stock Name" 
                   Text="{Binding Name, Mode=TwoWay}" FontSize="Medium" />

            <Entry Placeholder="Unit"  
                Text="{Binding Unit, Mode=TwoWay}" FontSize="Medium" Margin="0" />

            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>